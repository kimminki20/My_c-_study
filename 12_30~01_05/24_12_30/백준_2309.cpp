//백준 2309번 일곱 난쟁이
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
int main(){
    vector<int> v;
    for(int i=0; i<9; i++){
        cin >> v[i]; //난쟁이 9명의 키를 저장
    }
    sort(v.begin(),v.end()); //오름차순으로 정렬
    int sum = 0;
    while(sum == 100){
        int sum = 0;
    }
    
}